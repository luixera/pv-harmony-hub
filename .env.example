# ── Supabase ──────────────────────────────────────────────────────────────────
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# ── Google Maps ───────────────────────────────────────────────────────────────
VITE_GOOGLE_MAPS_API_KEY=

# ── Supabase Edge Functions (secrets — nunca no frontend) ─────────────────────
SUPA_SERVICE_ROLE_KEY=

# ── Claude API (Anthropic) ────────────────────────────────────────────────────
ANTHROPIC_API_KEY=

# ── Gmail API (fallback se não configurado via painel — opcional) ─────────────
# Configurar de preferência pelo painel /email-updates → Configurar
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_USER_EMAIL=
